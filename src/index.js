const app = new StorageBox;
AppAdapter.getActivities();
app.bindEventListeners();


   // let clickPlayer = () => {
   //  return (newPlayer.value ? addPlayer(newPlayer.value) : alert("cannot be nameless"));
   //  };
